# 开发日志 · 修仙模拟器（文字版）

> **给下一个接手的人 / AI：先读这一份，再动代码。**
> 本文件记录「为什么这么做」「怎么改才不出事」「还没做什么」。项目的历史改动细节在
> `.workbuddy/memory/` 的按日日志里（那是给会话记忆用的，可能被清理），本文件才是长期权威。

最后更新：**2026-09-16**
- 阶段一：单文件 → 40 模块化重构
- 阶段二：功法**技能化**——被动 3 格 / 主动 2 格、主动技带**冷却**与护体/虚空/冰封/灼烧/吸血/破防/反噬
- 阶段三：**材料 + 炼丹/炼器**——中间产物层、把随机产出变成可控产出（44 个模块）
- 阶段四（本轮）：**洞府 + 离线收益**——五处设施、关掉网页仍在运转、回来「闭关归来」一次结算（47 个模块）

---

## 0. 一句话现状

- 仓库根 = `D:\youxikuozhan\ai\zuoy\修仙模拟器`，**所有文件都不离开这个目录**
- **源码在 `src/`（47 个 ES 模块），成品是根目录的单文件 `xiuxian.html`**，由 `node scripts/build.mjs` 生成
- 上线形态：`site/` 目录静态托管（Vercel），线上 <https://www.xiuxianmoni.me/>
- 三道闸门：**构建校验** + `regression-test.js`（169 项断言）+ `scripts/verify-site.mjs`（74 项）——全绿才算改完

---

## 1. 架构决策：为什么不用 Vite / 打包器

**原方案（用户给的《阶段一完整方案》）是「拆成 Vite 多模块 + 部署 dist」。本轮没有照搬，理由如下：**

| 原方案会带来的问题 | 本轮的替代做法 |
|---|---|
| 需要 `npm install vite`（本机 npm 走代理、stdout 被吞，无法可靠验证） | 构建脚本**零依赖**，`node scripts/build.mjs` 直接跑 |
| `outputDirectory: dist` 会顶掉现有 `site/`（落地页 + `version.json` 实时更新机制全废） | 发布单元仍是 `site/`，`vercel.json` 不动，实时更新机制原样保留 |
| 根目录放 `package.json` 会让 Vercel 误判为前端框架项目并接管构建 | **不放 `package.json`**，Vercel 仍按根 `vercel.json` 当静态站发布 |
| 拆成真 ESM 后，`regression-test.js`（靠抽 `<script>` + `new Function` 跑）要整个重写，等于放弃 107 项既有断言 | 模块**带 import/export 写**，但构建时**剥掉 import/export 后拼回同一个作用域** → 既有测试脚本一行不用改，行为逐字节可验 |
| `dist/` 产物是多个文件，游戏就不再是「一个 HTML 双击即用」 | 成品仍是单文件，`file://` 双击可玩（模块版靠 file:// 会被 CORS 拦） |

**结论：多模块的「结构」拿到了，单文件分发的「形态」没丢，部署链路与测试体系一点没动。**
如果将来真要上 Vite 做 HMR 调试，见 §7「如果想换成真打包器」有明确的迁移代价清单。

---

## 2. 构建与目录

```
src/
├─ manifest.json      ← ★ 模块拼接顺序（加新模块必须登记在这里）+ 模板路径
├─ main.js            ← 启动 IIFE（boot）
├─ core/              ← utils 工具 · meta 元进度(META) · state 本局状态(S) · save 存档
├─ data/              ← 纯常量表，**只导出、不 import 任何东西**（防循环依赖的铁律）
│   qualities 品质 · items 器物名 · gongfa 功法 · realms 境界 · pills 丹药
│   monsters 妖兽 · grounds 历练地 · secrets 秘境 · mounts 坐骑 · heritage 传承
│   achievements 成就 · encounters 奇遇 · materials 材料 · recipes 丹方器方 · cave 洞府
├─ sys/               ← 玩法系统：log time character mount gongfa pills cultivate
│   breakthrough combat hunt dungeon shop sell encounter achievement rebirth craft cave
├─ ui/                ← theme sheet modal render(总渲染) + panels/{save,notice,gongfa,craft,cave}
├─ style/index.css    ← 全部样式与 64 个设计令牌
└─ template/          ← head.html（到 <style> 为止）· mid.html（到 <script> 为止）
                        script-open.txt（`"use strict";`）· tail.html（`</script></body></html>`）
```

### 构建做的事（`scripts/build.mjs`，约 120 行）

1. 读 `manifest.json` → 按 `order` 依次读模块
2. 每个模块：**删掉 `import` 行、删掉行首的 `export ` 关键字**
3. 拼成：`head.html` + `style/index.css` + `mid.html` + `"use strict";` + 各模块（中间插一行 `/* ── src/xxx.js ── */` 分隔注释）+ `tail.html`
4. 写 `xiuxian.html`，并把结果摘要写进 `_build_report.txt`

**关键性质（改动前必须理解）：** 拼接后所有模块的顶层声明共用**同一个作用域**——正是重构前单文件 `<script>` 的那个作用域。因此：

- `function` 声明全部提升，模块间互相调用不必关心先后（跨模块 import 只是「给编辑器和打包器看」的文档）
- 内联 `onclick="foo()"` 能直接用，**新增交互必须写成顶层 `function` 声明**（`const f = () => {}` 不会挂到作用域上给内联属性用）
- 拼接顺序只在**顶层立即执行的语句**上才有意义，全项目只有这几处：
  - `data/realms.js`：`REALMS = []` + `REALM_TREE.forEach(...)` + `MAX_LV = REALMS.length-1`
  - `data/achievements.js`：`ACH_BY_KEY` 的构建循环
  - `core/meta.js`：`META = blankMeta()`（`blankMeta` 在同文件上方）
  - `core/state.js`：`S = null`
  - `ui/panels/notice.js` 等：无
  → **把新模块插到 `data/` 之后任何位置都安全**；唯一硬规则是 `main.js` 留在最后（boot 要用到一切）。
- **直接给模块级绑定赋值是刻意的、正确的**（如 `core/save.js` 的 `S = {...}`、`sys/rebirth.js` 的 `META = blankMeta()`、`main.js` 的 `UID = 900000`）：
  因为是同一个作用域。**若将来改成真打包器**，这三处必须改成 `setS/setMETA/setUID`，否则报 `Assignment to constant variable`。

### 构建校验（改完必跑）

```powershell
node scripts/build.mjs                                  # 只构建
node scripts/build.mjs --check _baseline.html           # 构建 + 与参考文件做「一字未改」校验
```

`--check` 的原理：把成品与参考文件的脚本各自拆成**「非空行多重集」**比较——模块顺序变化不影响，
但任何一行被删改/重复都会被抓出来（会自动忽略构建器插入的分隔注释行）。

> 本轮拆分的**关键证据**：`--check` 对重构前的原始单文件报 `参考 2169 行 / 成品 2169 行 / 差异 0`，
> 即「一行代码都没改，只是搬了位置」。之后才在其上叠加新系统，行为变化全部由回归测试覆盖。
>
> 重构前的原始单文件仍可从历史里取回，用作任何一次重构的比对基准：
> `git show c7a907b:xiuxian.html > _baseline.html`（c7a907b 是拆分前的最后一次提交）。
>
> **构建器有一道硬守卫**（阶段二补）：成品里若残留任何 `import` / `export` 语句会直接抛错并打印行号。
> 起因是阶段二给 `sys/gongfa.js` 写了**跨行 import**，而 `stripImports` 当时只按行删 → 成品里残留 `import` →
> 游戏整体 `SyntaxError: Cannot use import statement outside a module`。现在 strip 支持跨行，且守卫兜底。

---

## 3. 怎么加一个新系统（手把手）

以「炼丹系统」为例：

1. **数据**：新建 `src/data/alchemy.js`，只导出常量（照抄 `data/gongfa.js` 的写法。**不要 import 任何东西**）
2. **逻辑**：新建 `src/sys/alchemy.js`，`import { S } from '../core/state.js'` 等；对外函数写成 `export function xxx(){}`
3. **界面**：新建 `src/ui/panels/alchemy.js`（弹层，用 `showModal(title, html, buttons, 'wide')`）；
   左栏/页签的锚点元素写在 `src/template/mid.html`
4. **登记**：把这 3 个文件按位置加进 `src/manifest.json` 的 `order`（数据放 `data/` 段，逻辑放 `sys/` 段，界面放 `ui/panels/`）
5. **接线**：在已有系统里插入调用点（例如 `stats()` 加一行、`renderAll()` 加一个 `renderXxx()`、`refreshShop()` 加一段货）
6. **存档**：`core/save.js` 的 `saveData()` 加字段、`restore()` 加**带默认值的容错读取**（老存档必须能直接读，缺字段补默认）
7. **持久化**（若需轮回保留）：`core/meta.js` 的 `blankMeta()` / `loadMeta()` / `applyMeta()` **三处都要加**，
   并且清洗器要能剔除非法值（参考 `gfSanitizeMeta`）
8. **测试**：`regression-test.js` 末尾加一节（照抄 P 节的写法），接口名要加进文件开头 `new Function(...)` 返回的 `G` 对象里
9. **构建 + 全量验证**：`node scripts/build.mjs` → `node regression-test.js xiuxian.html 回归测试结果.txt` → `node scripts/verify-site.mjs`
10. **文案**：游戏内「道法纲要」（`ui/panels/notice.js` 的 `showHelp`）+ 落地页 `site/index.html` 同步提一句

**UI 约定**：不新增配色。品质色用 `qName()/qBadge()/.qc0~.qc4`，提示色用 `.muted-sm/.dim`，
面板行可复用 `.heritage/.hr/.hn/.hl/.dots/.hb/.hd`，区块标题用 `.achsec`，滚动容器用 `.pb-scroll`。
测试会检查「渲染层不得输出 `style="color:#..."`」与「所有 `var(--x)` 必须有定义」。

---

## 4. 「功法 · 悟道录」系统（阶段一新增 · 阶段二技能化）

### 设计四原则（改它之前先读）

1. **永不改格局**：被动（心法）只 3 格、主动（术法）只 2 格 → 习得多寡 ≠ 战力，逼出**取舍**
2. **已习得永久留存**（存 `META.gongfa.learned`，轮回不灭）；**重数与装备槽归本局**（`S.gongfa`）
   —— 与「传承/成就永久、装备归零」的三轴设计一致，同时避免永久伤害通胀
3. **参悟同时消耗灵石与修为** → 让「冲境界」与「厚积功法」形成真实取舍（不是又一个纯增益）
4. **主动技带冷却 + 附加效果**（阶段二）：战斗节奏从「一直挥击」变成「何时放招」；
   基础「灵力斩」永远可用，所以**没有功法时战斗行为与旧版完全一致**

### 数据与数值

| 项 | 值 |
|---|---|
| 功法总数 | 30 部 = 15 心法（被动）+ 15 术法（主动），按大境界 5 段各 6 部 |
| 品阶 | 凡·灵·宝·仙·神（`t=0~4`），配色沿用器物的 `.qcN` |
| 参悟上限 | `GF_MAX = [3,5,7,9,9]` 重（按品阶） |
| 品阶系数 | `GF_TIER_M = [0.45,0.60,0.80,1.05,1.42]` |
| 被动基调 | `GF_XIN`：speed/body/blade/ward/insight/fate 六种，各自一套每重加成 |
| 主动基调 | `GF_SHU`：**gun 御器 / zhan 斩击 / lian 连击 / xue 血噬 / dun 守御 / ling 破灵 / huo 焚天 / bing 玄冰 / xu 虚空 / ji 因果** 十种 |
| 满重强度锚点 | 神品被动满 9 重 ≈ 修速 +14% / 血 +17% / 攻 +13.5% / 气运 +10.6（**刻意不越界到主宰级**） |
| 主动技倍率 | 0.8（虚空）～ 3.2（因果），满重再 ×(1 + 0.5×品阶系数) |
| 冷却 | 2 回合（御器）～ 7 回合（因果），写在各基调的 `cd` 上 |
| 参悟价 | 灵石 `(70 + t²×110) × 1.6^重 × (1+段×0.25)`；修为 `当前境界所需 × 2% × (重数+1)` |
| 藏经阁价 | `(120 + t²×300) × (1+段×0.22)`，每批货 2 部 |
| 学派 | `GF_SCHOOL` + `GF_PROFILE_SCHOOL`：基调 → 剑道/体修/法修/遁术/心法，**只做展示配色**（CSS 类 `.gfs-*` 走令牌，不写死颜色） |

### 来路（只有两条，刻意克制）

- **秘境贯通**：`completeDungeon()` 必掉 1 部未习得功法（按境界段加权）；已全习得则折算灵石
- **坊市 · 藏经阁**：`refreshShop()` 每次上架 `S.shopBook`（2 部），买下即习得（`gfLearn` 会自动填空槽）

### 接线点（要改功法数值/效果时看这里）

| 位置 | 作用 |
|---|---|
| `sys/character.js` 的 `stats()` | `gb = gfBonus()` → 气血/灵力/攻击/防御/暴击/修速/遁速 叠乘 |
| `sys/breakthrough.js` 的 `breakChance()` | `c += gfBonus().brk`（「悟道」类被动提供突破率） |
| `core/meta.js` 的 `fortune()` | `f += gfLuck()` —— **气运必须并进统一乘区，不要另开乘区** |
| `sys/combat.js` 的 `castSkill(arg)` | 主动技总入口：`arg` 为槽位 0/1（数字）或功法键（字符串）。冷却、灵力、段数、附加效果都在这里 |
| `sys/combat.js` 的 `tickTurn()` | 每个**玩家回合开始**结算：冷却递减、护盾/闪避计时、冰封递减、灼烧扣敌血 |
| `sys/combat.js` 的 `monsterTurn()` | 敌方回合：先判虚空闪避 → 再算伤害 → 冰封减半 → 护盾按比例减免 |
| `renderCombat()` | 状态条（护体/虚空/冰封/灼烧 余回合）+ 主动技卡（CD 中置灰显示「冷却中 · 余 N」）+ 未备术法时的引导卡 |
| `ui/render.js` 的 `renderGongfa()` | 左栏「功 法」面板：被动 3 行 + 主动 2 行 + 加成摘要 |
| `ui/panels/gongfa.js` 的 `openGongfa()` | 「悟道录」弹层：5 槽概览 + 已习得（装/卸/参悟）+ 未习得图鉴；槽满时 `gfUiPick()` 弹选择框 |
| `actionsShop()` | 坊市「藏经阁」段落 |
| `ui/render.js` 的 `renderUtil()` | 移动端抽屉入口 |

### 主动技的效果与结算（阶段二）

| 基调 | 倍率 | CD | 效果（`fx`） | 结算位置 |
|---|---|---|---|---|
| gun 御器 | 1.85 | 2 | `mpBack 5%` 回灵 | castSkill 末尾 |
| zhan 斩击 | 2.35 | 3 | `crit +8pt` | 命中判定 |
| lian 连击 | 1.35 ×2 段 | 3 | 两段各自结算暴击 | castSkill 循环 |
| xue 血噬 | 1.70 | 4 | `heal 35%` 吸血 | castSkill 末尾 |
| dun 守御 | 1.20 | 5 | `shield 55% ×2 回合` | monsterTurn（按比例减免） |
| ling 破灵 | 2.00 | 3 | `pierce 50%` 无视防御 | 命中判定（`m.def × (1-pierce)`） |
| huo 焚天 | 1.90 | 5 | `burn 3 回合`，每回合按我方攻击的 6%×4 扣血 | tickTurn |
| bing 玄冰 | 1.80 | 4 | `freeze 2 回合`（敌伤减半） | monsterTurn |
| xu 虚空 | 0.80 | 6 | `dodge 1 回合`（完全闪避） | monsterTurn 开头 |
| ji 因果 | 3.20 | 7 | `selfDmgPct 12%` 反噬自身 | castSkill 末尾 |

> 回合模型：**玩家每个动作 = 自己出手 + 敌方行动（monsterTurn）**。所以「虚空 1 回合」正好免掉本次施放引来的那一击。
> `fightPill()`（服丹）只触发敌方行动、**不推进** `tickTurn`（药不算一个完整回合）。

### 存档字段与迁移

```js
S.gongfa    = { passive:[k,null,null], active:[k,null], lv:{ 'gf_xxx': 3 } }  // 本局（v3）
S.shopBook  = ['gf_xxx','gf_yyy']                                             // 本局（藏经阁货架）
META.gongfa = { learned:['gf_xxx',...], best:{ 'gf_xxx': 9 } }                // 永久，轮回不灭
```

- `saveData()` 写 `v:3`
- **`gfSanitizeS()` 负责迁移**：读到旧形态 `{ xin, shu }` 会自动映射到 `passive[0] / active[0..1]`；
  越界重数按品阶截断、非法键剔除、类别混装（术法塞进被动槽）被拦下
- 老存档（完全没有 `gongfa` 字段）→ 补默认空槽，`stats()` 照常
- **存档键名不改**（`xiuxian_save_v2` / `xiuxian_meta_v2`），只升内部 `v`，这样老玩家的档不会丢

---

## 4.5 「材料 · 炼丹 / 炼器」系统（阶段三新增）

### 设计要点（改之前先读）

1. **补的是「中间产物层」**：此前打怪直接掉成品与灵石；现在多一条资源线——材料 → 炼制 → 丹药/法器。
   材料的价值不是「更省灵石」，而是**把随机产出变成可控产出**（坊市刷不出仙品武器，但可以反复炼）
2. **掉落与既有产出并行，不挤占概率**：妖兽的成品掉落分支一行没动，材料用**独立判定**另算；
   秘境 `dungeonSearch()` 的六个分支也一行没动，材料在 `dungeonForward()` 里作为**每层保底**
   → 老玩家的收益节奏完全不变
3. **材料是本局的**（`S.materials`，不存 META）——与装备/灵石同轴，轮回清零
4. **炼制消耗灵力 + 天数，不耗灵石**：让「出门打怪」与「留家开炉」形成节奏竞争
5. **失败返还 40% 材料**：有痛感但不劝退
6. **材料铺是「应急补差」而非主要来源**：买价刻意略高于成品（3 灵草 36 灵石 ≈ 一枚回春丹 34），
   缺一味不想出门时可以救急，但没人能靠买材料刷丹药套利。星辰砂与混沌石**不上架**

### 数据

| 项 | 值 |
|---|---|
| 材料 | 6 种：灵草(凡) · 兽皮(凡) · 妖丹(灵) · 玄铁(宝) · 星辰砂(仙) · 混沌石(神)；品阶沿用 `.qcN` + `qName()` 流光 |
| 丹方 | 4 个：回春丹(灵草3, 92%) · 聚灵丹(灵草2+妖丹1, 85%) · **归元丹**(灵草5+妖丹2, 72%, 坊市不售) · 破境丹(妖丹3+灵草5+星辰砂1, 55%) |
| 器方 | 3 个：兵器(玄铁3+妖丹2) · 护甲(玄铁3+兽皮5) · 法宝(星辰砂2+妖丹3)；均 `qiMin=2`（最低宝品） |
| 成功率 | `配方基础 + 丹道传承×1.5% + 境界差`（越级每档 -6%，超出每档 +2% 至多 +10%，夹在 5%~98%） |
| 境界锁 | 配方推荐段 > 当前大境界段 + 2 → 直接锁死（界面给原因） |
| 炼器品质 | `qiMin` 起步；`chance(12 + 气运×0.25)` 可 +1 阶；境界超推荐 4 档以上再 +1 阶；上限神品 |
| 消耗 | 灵力 `mpMax × mpF + 5`；天数 1~3 日/次（批量 ×n 线性放大） |

### 接线点

| 位置 | 作用 |
|---|---|
| `sys/combat.js` 的 `winFight()` | 材料**并行**掉落（独立 `chance`，放在 `checkAch()` 之前） |
| `sys/hunt.js` 的岩缝分支(`r<95`) | 额外掉灵草与玄铁（`rollSeamMats`） |
| `sys/dungeon.js` 的 `dungeonForward()` | **每层保底**材料（`rollSearchMats`） |
| `sys/dungeon.js` 的 `completeDungeon()` | 妖王材料包（`rollBossMats`） |
| `sys/craft.js` | `addMaterial/countMat/hasMats/costMats/refundMats` · 掉落表 · `craftPill/craftGear` · `matSanitize` |
| `ui/panels/craft.js` | 左栏「材 料」面板 + 坊市三段（丹房/器坊/材料铺）；操作函数 `crUi*` 前缀 |
| `ui/render.js` 的 `actionsShop()` | 在藏经阁之后、出售台之前插入三段 |
| `data/pills.js` | 新增「归元丹」（`craft:true` 只可炼不可买）+ `blankPills()` 由表派生药囊默认值 |

### 存档

```js
S.materials = { lingcao: 7, yaodan: 3 }        // 本局，轮回清零（saveData v:4）
```
- `restore()` 走 `matSanitize(d.materials)`：缺字段 → `{}`；非法键 / NaN / 负数一律剔除；小数取整
- 药囊默认值改为 `Object.assign(blankPills(), d.pills||{})`——**新增丹药自动补 0，老档不会缺字段**
- `usePill()` 的分支改成显式 `if/else if` 并补 `归元丹` 分支，未知丹药名会被安全拒绝
  （原来是 `else` 兜底当破境丹用，加新丹时会静默错乱）

---

## 4.6 「洞府 · 离线收益」系统（阶段四新增）

### 设计五条（改它之前先读）

1. **补的是「长期投资 + 离线积累」的锚点**：此前所有收益都是「在线时间 × 行动」，关掉网页就归零。
   洞府是第一条**离线仍在运转**的线，但不是主要来源——满级 26 小时约 936 灵石，一场妖王就上千。定位是「不亏，但别指望它」
2. **五处设施各管一条线**：灵田（灵草）· 聚灵阵（修速 + 自动凝石）· 丹房（成丹率）· 器坊（成器率）· 静室（离线上限）
3. **升级消耗灵石 + 天数**：天数走 `advance()`，与出门打怪形成节奏竞争（同一天你只能选一个）。五处全满约 51 万灵石 / 90 日
4. **离线上限 = 8 + 静室等级×2 小时，超出部分丢弃（不打折）** —— 这样升静室才有意义
5. **洞府归本局**（`S.cave`，轮回清零）——「知识不灭（功法/成就/传承），建设重来（洞府/装备/材料）」

### 数值

| 设施 | 每级 | 满级（9 级） | 满级累计 |
|---|---|---|---|
| 灵 田 | 灵草 +0.35/时（1 级 0.85） | 3.65/时 | 200·lv² → 5.7 万 · 9 日 |
| 聚 灵 阵 | 修速 +1% · 灵石 +4/时 | +9% · 36/时 | 300·lv² → 8.55 万 · 18 日 |
| 丹 房 | 成丹 +2% | +18% | 400·lv² → 11.4 万 · 18 日 |
| 器 坊 | 成器 +2% | +18% | 400·lv² → 11.4 万 · 18 日 |
| 静 室 | 离线上限 +2 时 | +18 时（总 26 时） | 500·lv² → 14.25 万 · 27 日 |

**注意成率封顶 0.98**：丹房/器坊对「本就好炼」的配方（回春丹 base 0.92）会被上限吃掉大半收益，
对破境丹（base 0.55）才是真提升。这是**刻意**的——设施帮的是短板，不是锦上添花。测试里用 `r_pojing` 断言 +10%。

### 接线点

| 位置 | 作用 |
|---|---|
| `sys/character.js` 的 `stats()` | `+ cvBonus().cult`（聚灵阵修速）。**不加进 `st.cultGear`** —— 那是器物修速，用于突破判定 |
| `sys/craft.js` 的 `finalRate(recipe, extra)` | 第二参数语义由「base」改为「extra 加成」。`pillCraftRate` 传 `cvBonus().pillRate`，`gearCraftRate` 传 `gearRate` |
| `sys/craft.js` 的 `craftPill` / `craftGear` | 成功率同样要带 `cvBonus()`，否则「显示 X% 实际按 Y% 炼」 |
| `core/save.js` 的 `saveData()` | `cave: cvSnapshot()` —— **lastTick 打上此刻**（见下） |
| `core/save.js` 的 `restore()` | `cave: cvSanitize(d.cave)`；老档缺字段 → `{facs:{}, lastTick: now}`，**不给白嫖** |
| `main.js` 的 `boot()` | `load()` 成功后**先** `cvSettleOffline()` **再** `renderAll()`；弹层放在声明与成就之后 |
| `ui/render.js` 的 `renderAll()` | `renderCave()` |
| `ui/panels/cave.js` | 左栏摘要 + `cvUiOpen()` 弹层 + `cvShowOfflineReport()`；操作函数 `cvUi*` 前缀 |
| `ui/panels/notice.js` 的 `showHelp()` | 洞府章节 |

### 存档

```js
S.cave = { facs:{ lingtian:3, juling:1 }, lastTick: 1758000000000 }   // 本局，轮回清零（saveData v:5）
```
- `cvSanitize(d)`：非法键 / 负数 / NaN 剔除，越界按 `max` 截断，**未来的时间戳一律丢弃**（时钟被改时 `now-lastTick` 会变负）

### ⚠ 本轮修掉的方案缺陷：在线挂机可刷离线收益

方案只在 `cvSettleOffline()` 里写回 `lastTick`。但 `save()` 每次渲染都跑，而 `lastTick` 只在结算时更新 ——
于是「在线玩 20 小时后刷新一次」会被算成 20 小时的离线收益（撞上限后白拿满额）。

**修法**：`saveData()` 输出 `cvSnapshot()`（浅拷贝 + `lastTick: Date.now()`）。
这样存档里的 `lastTick` 永远反映「最后一次活跃时刻」，离线时长 = 从最后活跃到下次打开，**在线时间不再计入**。
测试 `存档往返：v5 / … / lastTick 打上此刻` 守着这一点。

---

## 5. 本轮踩过的坑（别再踩）

### 5.1 测试脚手架的死循环（最花时间的一个）

`regression-test.js` 里原本有几处 `while (S().combat) G.fightAction('flee');`——**无上限**。
而 `sys/hunt.js` 第 25 行 `startFight(makeMonster(g.tier, chance(4)), ...)`：**历练有 4% 概率撞上妖王**，
妖王 `m.boss === true` 会在 `fightAction('flee')` 里直接 `return`（战斗中「遁走」卡也是锁住的，这是刻意设计）。
两者相遇 → 逃不走、打不动 → 死循环 → Node 4GB 堆爆（`FATAL ERROR: Reached heap limit`）。

**修法**：测试里加了统一的 `endCombat(max)` 帮助函数——按 `S().combat.m.boss` 决定用 `'atk'` 还是 `'flee'`，并一律带上限。

### 5.2 让回归结果可复现（重要改进）

测试里加了**固定随机种子**（LCG 覆写 `Math.random`）：

```powershell
node regression-test.js xiuxian.html 回归测试结果.txt           # 默认种子 20260915
$env:XX_SEED="9999"; node regression-test.js xiuxian.html 回归测试结果.txt   # 换一条随机路径
```

**任何新增玩法都会改变随机序列的消耗顺序**，从而让测试走到不同的分支。上面那个死循环就是这么被暴露的
（改动前 3 次复跑都过，改动后 2 次都炸）。加了种子之后：**用一批种子扫一遍**是发现隐藏死循环/边界的可靠手段，
本轮扫了 30 组种子，0 失败。

### 5.3 拆分工具自身的坑

- 我写了个一次性拆分器把原单文件机械切块。**它必须读「原始文件」，不能读「已构建的文件」**——
  第二次跑时我不小心让它读了构建产物，结果把构建器插入的分隔注释也当成了模块内容（一眼看出：模块文件里出现了重复的分隔注释）
- 扫描顶层语句时，若把 `const X = () => {...};` 的 `}` 误判为语句结束，会留下孤立的 `;`。
  修法：只有 `function/if/for/while/try/class/do/{` 开头的块状语句才允许以顶层 `}` 收尾
- **行尾注释会「搬家」**：`const Q_FLOW = 3;   /* 注释 */` 里 `;` 之后的内容若不吸进本语句，
  会被算作下一条语句的前导注释，构建后注释就跑到了别的函数头上。修法：语句结束后吸收「同一行」的尾随注释

### 5.5 顶层函数**不得重名**（阶段二踩到的最阴的坑）

构建后所有模块共用**同一个作用域**，所以「后出现的同名 `function` 声明会覆盖先前的」。
阶段二给面板写 UI 操作函数时，若沿用 `gfUpgrade` / `gfLearn` 这类与 `sys/gongfa.js` 同名的名字，
`ui/panels/gongfa.js`（在 `manifest.order` 里更靠后）就会**整体顶掉**逻辑层的版本，导致「点参悟扣钱但不升级」这种诡异现象。

**规则：模块内新增的顶层函数必须全局唯一。** UI 侧的操作函数统一加 `gfUi` 前缀（`gfUiToggle / gfUiUp / gfUiPick / gfUiSet / gfUiOff`），
需要复用的逻辑一律从 `sys/` 调用。加新模块时可先 `grep` 一下函数名。

### 5.6 改 API 名必须同步 `regression-test.js` 的接口清单

测试脚本用 `new Function(...)` 把所有顶层符号显式列进返回对象 `G`。阶段二把 `gfEquip` 改成
`gfToggleEquip` / `gfEquipAt` 后忘了改清单 → 整个沙箱在**创建阶段**就抛 `ReferenceError: gfEquip is not defined`
（表现为「通过 0 项」或退出码异常）。**改任何导出名，同步改那份清单。**

### 5.7 禁止命名空间 / 默认导入（阶段三踩到）

构建器会把 import 整行**剥掉**。因此 `import * as CR from '...'` 之后写的 `CR.craftPill(...)` 一定崩
（构建期能过，运行到那条路径才 `ReferenceError`）；默认导入 `import X from '...'` 同理。
**只能用具名导入 `import { a, b } from '...'`。**

现在构建器会**在构建期拦下**这两种写法，并打印文件名与修法（`scripts/build.mjs` 的 bad-import 检查）。

### 5.8 构建器对 CRLF 与 BOM 的容忍（阶段三补）

`stripImports` 原来的行尾锚点是 `[ \t]*$`——遇到 **CRLF** 时 `\r` 会让它匹配失败，import 就剥不掉；
文件开头有 **BOM**（Windows 编辑器常见）还会让"按行首匹配"的检查全部失效。
现在：`read()` 去掉 BOM；import 正则补 `\r?`；模块级检查正则不再依赖 BOM 前的行首。
**这类问题在我这边（LF 无 BOM）看不出来，换编辑器或换平台就会踩**——所以两道守卫都留着。

### 5.9 本机环境（每次都会撞上）

- **PowerShell 工具会吞掉原生命令的 stdout**：一律让脚本写文件（`_xxx.txt`）再读
- **`git commit -m "中文"` 会写坏提交信息**（PS 5.1 按控制台代码页编码参数）→ 信息写 UTF-8 文件后 `git commit -F`
- **验线上是否为新版**：`fetch` 线上 `xiuxian.html` 的 sha256 与本地 `site/xiuxian.html` 逐字节比对
- 沙箱里 `node:dns` 必然 `ECONNREFUSED`；查 DNS 用 PowerShell 的 `Resolve-DnsName`
- 代理偶发 502 / 超时**不代表站点故障**，等十几秒重试即可
- PS 安全策略会拦 `Net.WebClient`（报 "Download cradle"）→ 抓线上文件改用 Node `fetch`
- PS 的 `Remove-Item` 传多个文件 + 非 ASCII 路径时**可能静默不生效**（退出码 0 但文件还在）→ 用 Node `unlinkSync` 清理更可靠
- 小坑：`scripts/release.mjs` 里既有 `const build = ...`（版本号），改脚本时别再声明同名变量

---

## 6. 常用命令速查

```powershell
# 构建（src/ → xiuxian.html）
node scripts/build.mjs

# 构建 + 与参考文件做「一字未改」校验
node scripts/build.mjs --check <参考文件.html>

# 游戏回归测试（169 项断言，固定种子）
node regression-test.js xiuxian.html 回归测试结果.txt

# 换一条随机路径复跑（排查偶发问题）
$env:XX_SEED="9999"; node regression-test.js xiuxian.html 回归测试结果.txt

# 站点自检（74 项：HTTP / SEO / a11y / 性能 / 缓存头 / 部署配置）
node scripts/verify-site.mjs

# 一键发版（内部先构建，再同步 site/ + version.json + 首页版本 + sitemap）
node scripts/release.mjs "本次更新说明"

# 推上线
git add -A; git commit -F _commitmsg.txt; git push
```

Node 路径（本机受管版本）：`C:\Users\Lenovo\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`

---

## 7. 还没做的（建议下一轮）

| 项 | 说明 |
|---|---|
| **数值重平衡（优先）** | 阶段二把被动从 1 格扩到 3 格、又加了两个可放技能，战力曲线整体上移。原方案的建议是「先上线观察，玩三天再调」——若确需收紧，候选旋钮：`makeMonster()` 的 hpMax/atk/def（×1.4~1.6）、`breakChance()` 基础值（74→68）、`expNeed` 底数（140→165）、`gfCost` 系数（×1.5）。**一次只动一个，动完跑 35 组种子** |
| **成就不含功法** | 成就仍是 42 项（`ACHIEVEMENTS`）。加功法类成就需同步改：成就表、`regression-test.js` 的「42 项」断言、`showHelp` 与落地页的「42 项」文案 |
| **主动技与坐骑遁速无联动** | 遁速目前只影响逃跑成功率与赶路耗时，未参与技能（例如「虚空」可考虑随遁速延长闪避回合） |
| **`META.gongfa.best` 只记最高重数** | 未做「历代修行记录」展示；图鉴里也没有「已圆满」的汇总统计 |
| **无 `core/bus.js`** | 原方案里的事件总线没有采纳（YAGNI：当前模块间是直接 import 调用，加总线只是多一层）。等真出现「一个动作要通知多个系统」的需求再加 |
| **未做资源分片** | 单文件约 261 KB。若继续变大，可考虑构建时把 CSS 拆成外链（但会破坏 `file://` 双击可玩的形态） |
| **材料暂无回收出口** | 材料目前只能炼，不能卖；后续可做「工坊委托」或按品阶折价回收，避免溢出库存无意义堆放 |
| **洞府无「一键收菜」外的交互** | 目前只有升级。可加：灵田「催熟」（耗灵力换即时产出）、静室「闭关」（主动消耗天数换修为倍率）、洞府灵气值（影响全局产出）|
| **洞府不入成就** | 成就仍是 42 项；可加「洞府初成／五处齐备／离线满收一次」等（注意同步测试与两处「42 项」文案）|
| **未做材料成就** | 成就仍是 42 项；可加「首次成丹／炼出仙品／累计炼制 N 次」等（注意同步测试与两处「42 项」文案） |
| **域名口径不一致** | 站内 canonical/og:url/JSON-LD/robots/sitemap 指向根域名 `xiuxianmoni.me`，而根域名 308 跳到 www。二选一：① Vercel 把根域名设为 Primary（推荐，代码不动）② 全改成 www |
| **`.me` 能否 ICP 备案未验证** | 指向海外平台不需要备案；**验证前不要买国内服务器** |
| **想换真打包器（Vite）时** | 需要：① `package.json`（会让 Vercel 改变项目类型，必须在 `vercel.json` 里显式压回静态发布）② 把 `S=`/`META=`/`UID=` 三处直接赋值改成 `setS/setMETA/setUID` ③ 重写 `regression-test.js` 的加载方式（改为动态 `import()` 单测各模块）④ 接受单文件分发形态结束（或再写个内联构建器把 dist 合回单文件） |

---

## 8. 项目不变量（不要破坏）

1. **零依赖、零 `npm install`**：任何人 clone 下来 `node scripts/build.mjs` 就能改能跑
2. **单文件分发**：`xiuxian.html` 必须能 `file://` 双击打开可玩
3. **无第三方请求**：没有 CDN、没有埋点、没有外链字体（`verify-site.mjs` 会拦）
4. **存档永远向前兼容**：老存档必须能在新版直接读（缺字段补默认，非法值剔除）；三个存档键 `xiuxian_save_v2` / `xiuxian_slot_{0,1,2}_v2` / `xiuxian_meta_v2` **不要改名**
5. **气运只有一个乘区**：`fortune()`（成就 + 气运传承 + 被动功法）。新增任何「福缘类」加成并进它，不要另开
6. **顶层函数名全局唯一**（构建后同处一个作用域，重名会静默覆盖；UI 侧一律加 `gfUi`/`crUi` 之类前缀）
7. **新增玩法要留「无功法也照旧」的退路**：基础「灵力斩」永远可用、未装备主动时战斗面板给引导卡；
   这样老存档、新号、测试都能走通
8. **只能用具名导入**（`import { a } from '...'`）：命名空间 / 默认导入会被构建器剥掉整行，
   而用法留在代码里 → 运行时才炸。构建器已在构建期拦下这两种写法
9. **新增产出要与既有产出「并行」而不是「挤占」**：材料就是独立判定加的，老玩家的掉落概率一分未变
10. **游玩声明口径统一**：游戏内 `noticeHtml()` 与落地页 `#notice` 必须一致（`regression-test.js` O 节 6 项断言守着）
11. **发版必须走 `release.mjs`**：它内部先构建，再重算 sha256 写 `version.json`——这是「实时更新」的唯一触发点
12. **不要在左栏之外新增第 6 个页签**：`#tabs` 移动端网格写死 `repeat(5,1fr)`。洞府与材料都走「左栏面板 + 坊市页内分段」
13. **存档时刷新 `S.cave.lastTick`**：离线时长必须 =「最后活跃 → 下次打开」，否则在线挂机数小时后刷新可白刷满额离线收益。
    `saveData()` 走 `cvSnapshot()` 保证这一点（测试 S 节守着）
14. **离线结算必须在 `renderAll()` 之前**：它改灵石与材料，先算后画才能一次画对；弹层则放在声明与成就之后，免得打断它们
