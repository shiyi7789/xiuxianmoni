# 修仙模拟器 · 部署上线与「实时更新」指引

> 目标：把 `site/` 目录发布成一个可访问的网址，并且**只更新游戏文件就能让线上的网站自动变成最新版**——包括正在玩的访客。

---

## 一、你拿到的是什么

```
修仙模拟器/                    ← 仓库根目录（不直接对外）
├─ src/                        ← ★ 游戏源码：40 个 ES 模块（日常改这里）
│  ├─ core/ data/ sys/ ui/      ← 工具·数据·玩法·界面
│  ├─ style/index.css           ← 全部样式与设计令牌
│  └─ manifest.json             ← 模块拼接顺序
├─ xiuxian.html                ← ★ 构建产物（单文件成品，由 build.mjs 生成，不要手改）
├─ site/                       ← ★ 发布目录，平台上线的就是这个目录
│  ├─ index.html               ← 落地页（响应式，33 KB，零外部请求）
│  ├─ xiuxian.html             ← 游戏本体副本（由发版脚本自动同步）
│  ├─ version.json             ← 版本清单（实时更新的核心，脚本自动生成）
│  ├─ _headers                 ← 缓存与安全响应头（Cloudflare Pages / Netlify 通用）
│  ├─ robots.txt / sitemap.xml ← SEO
│  ├─ 404.html
├─ scripts/
│  ├─ build.mjs                ← 零依赖构建：src/ → xiuxian.html
│  ├─ release.mjs              ← 一键发版：构建 + 同步游戏 + 生成版本号 + 回填首页
│  ├─ verify-site.mjs          ← 上线前自检：74 项 HTTP/SEO/a11y/性能检查
│  └─ connect-github.ps1       ← 一键连远端仓库
├─ 开发日志.md                  ← 架构决策与交接说明（换人接手先读它）
├─ vercel.json                 ← Vercel 专用配置（已声明 outputDirectory: site）
└─ .gitignore
```

**你日常只需要改 `src/` 下的模块，然后跑一条命令、推一次代码。** 其余文件都是自动的。
`xiuxian.html` 是构建产物——手改它会在下一次构建时被覆盖，且 CI 会拦下这种不一致。

---

## 二、三条上线路径，选一条

| | 路径 A · 拖拽上线 | 路径 B · Git 自动部署 ★推荐 | 路径 C · 自建 VPS |
|---|---|---|---|
| 平台 | Cloudflare Pages / Netlify Drop | Cloudflare Pages / Vercel / Netlify | Nginx + 自己的服务器 |
| 上线耗时 | 1 分钟 | 首次 5 分钟，之后每次 30 秒 | 首次 1 小时 |
| 更新方式 | 手动拖一次 | **`git push` 自动** | `git pull` + 重载 |
| 成本 | 免费 | 免费 | 服务器费用 |
| 适合 | 先看效果 | **长期维护，强烈推荐** | 要完全可控 |

> 建议：**先用路径 A 拿到一个临时网址确认效果，再按路径 B 建仓库长期维护。** 两条路径都只需要 `site/` 这一个目录，配置完全兼容。

---

## 二·五、为什么这个站点不需要服务器（也不用花钱）

**结论：你不需要买 ECS、轻量应用服务器、云虚拟主机或任何 VPS。** 一个已经注册的域名 + 免费静态托管就够了。

原因在于这个站**没有后端**。传统网站必须用服务器来承担的那些事，这里一件都不需要：

| 传统网站需要服务器的原因 | 这个站点怎么办到的 |
|---|---|
| 要跑后端程序（PHP / Node / Java / Python） | **没有后端**。游戏逻辑全部在浏览器里执行——就是一个约 204 KB 的 `xiuxian.html` |
| 要数据库保存用户进度 | 进度存在**访客自己浏览器的 `localStorage`** 里。服务端零存储、零数据库 |
| 要处理登录、会话、Cookie | 没有账号系统，不需要 |
| 要托管静态文件并承担带宽 | 交给 CDN 边缘节点。Cloudflare 全球 300+ 节点、Vercel 100+ 节点，就近返回 |
| 要自己申请、安装、续期 HTTPS 证书 | 平台自动签发 + 自动续期，全程零操作 |
| 要运维：打补丁、监控、防 DDoS、扩缩容 | 平台托管。你唯一要做的运维动作是 `git push` |
| 要备案才能对外提供服务 | 海外平台（Vercel / Cloudflare Pages）**不需要 ICP 备案** |

**代价（必须如实知道的三点）**

1. **存档只存在访客本机**。换设备、换浏览器、清缓存都会丢档。这也是游戏里专门做了「存档码」和「存档文件导出/导入」的原因——换设备时导出再导入。
2. **做不了需要服务端的功能**：排行榜、账号体系、多人联机、云端同步存档，这些都需要服务器，现在没有。
3. **免费套餐有额度**：Cloudflare Pages 静态请求与带宽不限量；Vercel Hobby 每月 100 GB 流量、100 次部署。个人游戏站远远用不完。

**什么时候才真的需要服务器**

- 想做账号系统或云端存档、排行榜 → 需要一个后端（可先用 Serverless 函数，仍不必买服务器）
- 要求国内机房级别的访问速度 → 需要国内主机，**且必须完成 ICP 备案**
- 要绑定国内 CDN / OSS 自定义域名 → 同样需要备案

在以上情形出现之前，**保持零服务器成本**是最划算的选择。

---

## 三、路径 A：拖拽上线（1 分钟拿到网址）

1. 打开 <https://app.netlify.com/drop>（或 Cloudflare Dashboard → Workers & Pages → Create → Pages → Upload assets）。
2. 把 **`site` 整个目录**拖进去（拖目录本身，不是里面的文件）。
3. 等 10~20 秒，得到形如 `https://random-name-1234.netlify.app` 的网址。
4. 打开验证：落地页能开、点「开始修行」能进游戏、玩两下刷新进度还在。

**缺点**：每次更新都要重新拖一次，而且拖拽上传不会读到 `_headers`（部分平台忽略），缓存策略不生效。所以只适合试水。

---

## 四、路径 B：Git 自动部署（推荐，这才是真正的「实时更新」）

### 第 1 步 · 本地建仓并首次提交

在项目根目录 `D:\youxikuozhan\ai\zuoy\修仙模拟器` 执行：

```powershell
git init -b main
git add -A
git commit -m "chore: 初始化站点（落地页 + 游戏本体 + 部署配置）"
```

`.gitignore` 已经把临时输出、测试产物和编辑器文件排除掉了，`git status` 里不该出现 `_probe.txt`、`node_modules` 之类。

### 第 2 步 · 推到 GitHub

先在 GitHub 上建一个**空仓库**：

> ## ⚠️ 务必不要勾选任何初始化选项
> 创建页面上的 **"Add a README file"**、**.gitignore**、**license** 三个复选框**全部留空**。
>
> **为什么**：勾选任何一个，GitHub 都会在远程替你生成一个 `Initial commit`。这个提交与本地历史**完全不相干**（`git merge-base` 为空），于是 `git push` 会被拒绝：
>
> ```
> ! [rejected]  main -> main (fetch first)
> ```
>
> **症状**：推送"好像跑了"，但打开仓库只看到一个 `README.md`，你所有文件都不见了。
>
> **补救**：直接跑升级后的 `scripts/connect-github.ps1`——它会自动识别这种不相干历史，列出远程那个占位提交，询问确认后用 `--force-with-lease` 覆盖（只覆盖远程多出来的提交，不动你的代码）。

然后执行：

```powershell
git remote add origin https://github.com/<你的用户名>/<仓库名>.git
git push -u origin main
```

### 第 3 步 · 平台连接仓库

**Cloudflare Pages**（免费、国内访问相对友好）：
1. Dashboard → Workers & Pages → Create → Pages → **Connect to Git**
2. 选仓库 → 分支 `main`
3. **Framework preset: `None`**
4. **Build command: 留空**（本项目零构建）
5. **Build output directory: `site`** ← 关键
6. Save and Deploy

**Vercel**：
1. Add New → Project → Import 仓库
2. Framework Preset: `Other`
3. Build Command 留空、Output Directory 会自动读到 `vercel.json` 里的 `site`

**Netlify**：
1. Add new site → Import an existing project
2. Build command 留空，**Publish directory 填 `site`**

### 第 3.5 步 · 一条命令完成「身份 + 远程 + 自检 + 推送」

仓库已经在本地建好并提交（2 次提交、20 个文件）。你在 GitHub 网页建完空仓库后，**只需跑一条命令**：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\connect-github.ps1 -User 你的GitHub用户名 -Repo 仓库名
```

它会依次做四件事：

1. 设置本仓库的提交身份（默认用 `<用户名>@users.noreply.github.com`，只作用于本仓库，不动全局配置）
2. 用 `--amend --reset-author` 把首提交的作者改成你（所以**务必在首次推送前跑**）
3. 调用 `scripts/verify-site.mjs` 做推送前自检，**不通过就中止，不会把坏版本推上去**
4. `git push -u origin main`，首次会弹出 Git Credential Manager 登录窗口，授权一次即可

> 想用真实邮箱而不是匿名邮箱，加 `-Email you@example.com`；想让仓库公开，加 `-Visibility public`。

### ⚠️ 关于 GitHub Pages 的一个重要限制

如果你打算直接用 GitHub Pages（免去注册第三方平台），有两件事必须先知道：

1. **免费账号的 Pages 只支持 public 仓库**。要保留 private，请改用 Cloudflare Pages / Vercel——它们都支持私有仓库，配置同样只有「Build command 留空 + 输出目录 `site`」。
2. **GitHub Pages 无法自定义响应头**，它的 HTML 默认缓存约 10 分钟。这意味着本文第五节讲的**机制 2（缓存策略）在 Pages 上失效**，更新最长会延迟 10 分钟才能被新访客拿到——不再满足你最初要的「实时更新」。

所以：

| 你的目标 | 推荐 |
|---|---|
| 只要有个能访问的网址，不介意更新有几分钟延迟 | GitHub Pages（仓库设为 public，Settings → Pages → Source 选 **GitHub Actions**） |
| 要真正的「改完即生效」 | **Cloudflare Pages / Vercel 连接同一仓库**（可保持 private，`site/_headers` 与根 `vercel.json` 才会被读取） |

仓库里已经放好 `.github/workflows/pages.yml`，一旦把 Pages 的 Source 切成 GitHub Actions，之后每次 push 都会自动发布、无需其他配置。

### 第 4 步 · 绑定域名 `xiuxianmoni.me`

域名已在阿里云注册并完成实名认证，DNS 由**阿里云云解析**托管。

#### 4.0 先做一个决定：要不要备案

| 方案 | 备案 | 上线耗时 | 国内访问速度 | 成本 |
|---|---|---|---|---|
| **指向海外平台（Vercel / Cloudflare Pages）** | **不需要** | 5~30 分钟 | 一般（可访问，但不如国内机房） | 免费 |
| 指向国内服务器（阿里云 OSS / CDN / ECS） | **需要 ICP 备案** | 1~3 周 | 快 | 有费用 |

> ⚠️ **关于 .me 能否备案，公开信息是矛盾的**：阿里云开发者社区有答复明确把 `me` 列入"未批复后缀"，而部分第三方备案服务商的列表里 `me` 又标注为"可以备案"。**唯一权威的判断方式**是在 [阿里云备案控制台](https://beian.aliyun.com/) 里输入域名，系统会自动校验后缀是否可备案；或查工信部"域名注册管理机构审批情况"的【业务范围】列。
>
> 因此在验证之前，**不要**先买国内服务器。本指引推荐先走免费方案把站点跑起来。

#### 4.1 推荐路径：Vercel（不用动阿里云 DNS）

**为什么选 Vercel 而不是 Cloudflare Pages**：根域名（`xiuxianmoni.me`）按 DNS 规范**不能使用 CNAME**（RFC 1034——根域名必须存在 SOA/NS 记录，而 CNAME 不能与其它记录共存）。Vercel 对根域名提供的是标准 **A 记录**，因此可以保留阿里云的 NS 与现有解析记录，改动最小。Cloudflare Pages 则要求把域名的 NS 迁到 Cloudflare（除非用它的付费版 CNAME 接入）。

**步骤**

1. Vercel → Add New → Project → 导入 GitHub 仓库 `shiyi7789/xiuxianmoni`
   - Framework Preset 选 `Other`
   - **Build Command 留空**（项目零构建）
   - Output Directory 会自动读到仓库根 `vercel.json` 里的 `site`
2. 部署完成后：Project → **Settings → Domains** → 输入 `xiuxianmoni.me` → Add
3. ⚠️ **以 Vercel 域名卡片上显示的值为准**，去阿里云云解析添加记录（下表是通用值，新项目可能分到别的 anycast IP）：

   | 记录类型 | 主机记录 | 记录值 | TTL |
   |---|---|---|---|
   | A | `@` | `76.76.21.21`（**以卡片为准**，可能是 `216.198.79.1` 等） | 600 |
   | CNAME | `www` | 卡片上的 CNAME 目标（形如 `xxxx.vercel-dns-xxx.com`） | 600 |

4. 阿里云云解析里**删掉根域名上遗留的其它 A 记录**（旧主机商的记录会导致流量分散、校验失败）。
5. **不要给根域名加 AAAA 记录**：Vercel 的自定义域名不支持 IPv6，多出来的 AAAA 会让 SSL 签发卡住。
6. 等 5~30 分钟，回到 Vercel，域名卡片变成 **Valid Configuration**，HTTPS 证书自动签发。

**验证**

```powershell
nslookup xiuxianmoni.me
curl -I https://xiuxianmoni.me/                       # 期望 200 + cache-control: max-age=0
curl -I https://xiuxianmoni.me/xiuxian.html
curl https://xiuxianmoni.me/version.json              # 期望看到 v / gameSha256
```

#### 4.2 备选路径：Cloudflare Pages（需改 NS）

1. Cloudflare → Add a site → 填 `xiuxianmoni.me` → 选 Free 套餐
2. Cloudflare 会给你两个 NS 地址，去**阿里云域名控制台 → DNS 修改**，把 NS 改成这两个
3. 等 NS 生效（几分钟到几小时）。**注意：改 NS 后，阿里云云解析里的记录不再生效**，需要把邮件 MX 等记录在 Cloudflare 重新配一遍
4. Cloudflare → Workers & Pages → Create → Pages → Connect to Git → 选仓库
   - Build command 留空、Build output directory 填 `site`
5. Pages → Custom domains → 添加 `xiuxianmoni.me` 和 `www.xiuxianmoni.me`，Cloudflare 会自动配好解析与证书

Cloudflare 的优势是免费无限带宽、`site/_headers` 原生生效，且免费套餐在国内的可达性通常略优于 Vercel。

#### 4.3 域名切换后已经帮你改好的东西

以下位置原本是占位域名 `example.com`，现已全部替换为 `xiuxianmoni.me`：

- `site/index.html`：`rel="canonical"`、`og:url`、JSON-LD 的 `url`
- `site/robots.txt`：`Sitemap:` 行
- `site/sitemap.xml`：两个 `<loc>`

`scripts/verify-site.mjs` 新增了**域名守卫**：如果这些位置还残留 `example.com`，自检会直接失败，避免带着占位域名上线（那会把 SEO 权重指向别人的站点）。

#### 4.4 社交分享图（可选，尚未设置）

`og:image` 目前**故意留空**。原因：指向一个不存在的 `og.png` 会让分享卡片渲染成空白图，比不设置更糟。

补法：放一张 **1200×630 的 PNG** 到 `site/og.png`，然后在 `site/index.html` 的 `<head>` 里加回这四行：

```html
<meta property="og:image" content="https://xiuxianmoni.me/og.png">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta name="twitter:image" content="https://xiuxianmoni.me/og.png">
```

同时把 `twitter:card` 从 `summary` 改为 `summary_large_image`。加好后 `verify-site.mjs` 会自动校验该文件是否存在。

---

## 五、核心问题：怎么做到「更新游戏就实时更新」

很多人以为「把文件传上去」就等于更新了，结果发现用户还是看到旧版本。真正要做到实时，必须**三个机制同时成立**，缺一不可：

### 机制 1 · 部署触发：改动自动上线

`git push` → 平台监听仓库 → 自动拉取 `site/` 并重新发布。这是「自动」，不是「实时」。

### 机制 2 · 缓存策略：HTML 绝不能被长期缓存 ← 最容易被忽略的坑

这是**唯一会让更新彻底失效**的环节。浏览器和 CDN 都会缓存文件，如果 `xiuxian.html` 被缓存 24 小时，那你换了文件用户也要 24 小时后才看得到。

本项目的策略（写在 `site/_headers`，Cloudflare/Netlify 会自动读取；Vercel 读 `vercel.json`）：

| 文件 | 响应头 | 原因 |
|---|---|---|
| `/`、`/index.html`、`/xiuxian.html` | `public, max-age=0, must-revalidate` | HTML 就是「首屏 + 全部逻辑」，必须每次回源校验；没变就返回 304，比重新下载还快 |
| `/version.json` | `no-store, no-cache, must-revalidate` | **绝不能缓存**，否则前端永远探测不到新版本 |
| `/robots.txt`、`/sitemap.xml` | `max-age=86400` | 纯元信息，允许缓存一天 |

> 为什么不直接 `no-store` 全部？因为 `max-age=0, must-revalidate` 能吃到 304 复用，比每次全量重下更省流量、更快。

### 机制 3 · 前端版本探测：让**正在玩的人**也自动拿到新版

只做前两步，刷新页面的人会看到新版，但已经打开页面、正在打坐的人不知道自己拿到了旧代码。落地页的 `index.html` 里做了这件事：

1. 页面加载时 `fetch('version.json')`（带 `cache: 'no-store'` 和不重复的查询串，绕开一切缓存），记住当前版本号。
2. **每 60 秒**轮询一次，并在「切回标签页」「网络恢复」时立即补查一次。
3. 一旦发现版本号变了，右下角弹出提示条：**「发现新版本 xxx，刷新即可载入（进度不会丢）」**。
4. 点「刷新」→ 内嵌试玩的 iframe 会带上 `?v=新版本号` 重新加载，强制取到新文件。

**为什么刷新不会丢进度**：游戏每次操作都会把状态写进浏览器 `localStorage`，刷新等价于「关掉再打开」，存档原样还在。这一点也写进了落地页的 FAQ。

### 串起来看

```
你改 src/ 下的模块
   ↓  node scripts/build.mjs              ← 构建：模块 → 单文件 xiuxian.html
   ↓  node scripts/release.mjs "说明"      ← 同步 + 生成新版本号
   ↓  git add -A && git commit && git push  ← 机制 1：触发平台重新部署
平台约 10~30 秒后发布新文件
   ↓  HTML 响应头 max-age=0               ← 机制 2：新访客立刻拿到新版
   ↓  version.json 无缓存
   ↓  已打开页面 60 秒内轮询到版本变化      ← 机制 3：旧访客收到升级提示
```

---

## 六、日常发版流程（三条命令）

改完 `src/` 下的模块后，在项目根目录执行（`release.mjs` 内部会先跑构建，故不必单独构建）：

```powershell
# 1) 构建 + 同步游戏到站点 + 生成新版本号 + 回填首页版本 + 刷新 sitemap
node scripts/release.mjs "本次更新说明，会写进 version.json"

# 2) 自检（74 项，必须 0 失败）
node scripts/verify-site.mjs

# 3) 推送（触发平台自动部署）
git add -A
git commit -m "release: 本次更新说明"
git push
```

脚本输出会告诉你新版本号，例如：

```
0) 已构建 模块数: 40
1) 已同步 xiuxian.html  208916 B  sha256:ea60643cb601
2) 已写入 version.json  v=2026.09.15-2228  build=20260915-222840
3) 已回填 index.html 页脚版本号 → 2026.09.15-2228
4) 已刷新 sitemap.xml lastmod → 2026-09-15
```

> **每次改完游戏，一定要跑 `release.mjs`**。它会重算 sha256 并写入 `version.json`——这是让机制 3 生效的唯一触发点。忘了跑，线上文件会更新但已打开的页面收不到提示。

### 上线前自检

```powershell
node scripts/verify-site.mjs
```

它会**本地起一个真实静态服务并 HTTP 抓取页面**，核对 74 项：

- HTTP 层：所有页面 200、未知路径 404、两个关键的缓存头是否正确
- 实时更新链路：`version.json` 的 sha256 / 字节数是否与磁盘上的游戏文件**逐字节一致**、首页版本号是否已更新
- 语义与 SEO：DOCTYPE、`lang`、viewport（含 `viewport-fit=cover`）、标题与描述长度、canonical、OG 三件套、JSON-LD、唯一 `h1`、标题层级无跳级
- 无障碍：skip link、图标按钮 `aria-label`、`details/summary` 键盘可操作、`:focus-visible` 保留、`prefers-reduced-motion`
- 性能：零第三方子资源、无渲染阻塞外链、`100dvh`、iframe 延迟加载与宽高比预留（防 CLS）、首页与游戏体积
- 部署配置：`_headers` 的 CSP / nosniff / 禁缓存、`vercel.json` 可解析且输出目录正确

**期望结果：`通过 74 项，失败 0 项`。** 有任何 `FAIL` 都不要推。

---

## 七、上线后验证（复制即用）

```powershell
# 确认 HTML 没被长缓存（关键：看 cache-control 是不是 max-age=0）
curl -I https://你的域名/xiuxian.html

# 确认 version.json 禁止缓存
curl -I https://你的域名/version.json

# 确认版本号已经是新的（和你 release 输出的一致）
curl https://你的域名/version.json

# 确认安全头到位
curl -I https://你的域名/ | Select-String "content-security-policy|x-content-type-options|x-frame-options"
```

再手动过一遍：落地页打开无横向滚动条 → 手机宽度下排版正常 → 点「开始修行」游戏能跑 → 玩两下刷新进度还在 → 手机浏览器底部导航栏正常 → 系统切到深色模式页面跟着变（或点右上角 ☾ 手动切）。

---

## 八、上线自检清单

**功能**
- [ ] `/` 返回 200，落地页无排版崩坏、无横向滚动
- [ ] `/xiuxian.html` 返回 200，游戏可玩
- [ ] 玩几步后刷新，进度仍在（localStorage 正常）
- [ ] 落地页「立即进入」能加载内嵌游戏
- [ ] 故意访问一个不存在的路径，显示 404 页面

**移动端**
- [ ] ≤820px 时游戏是底部导航 + 上滑抽屉
- [ ] 刘海屏机型底部不被遮挡（`safe-area` 生效）
- [ ] 触摸目标不小于 44px，按钮不会点不中
- [ ] 移动端地址栏伸缩时布局不跳（`100dvh` 生效）

**实时更新**
- [ ] `curl -I /xiuxian.html` 的 `cache-control` 是 `max-age=0`
- [ ] `curl -I /version.json` 的 `cache-control` 含 `no-store`
- [ ] 跑一次 `release.mjs` 后，`/version.json` 的 `v` 变了
- [ ] **保持落地页开着**，推一次更新，60 秒内应弹出「发现新版本」

**SEO / 无障碍**
- [x] `canonical` / `og:url` / JSON-LD `url` / `robots.txt` / `sitemap.xml` 已全部指向 `xiuxianmoni.me`（`verify-site.mjs` 已自动守卫，残留 `example.com` 会直接失败）
- [ ] 绑定域名后 `curl -I https://xiuxianmoni.me/` 返回 200 且 `cache-control: max-age=0`
- [ ] `robots.txt` 的 Sitemap 地址已换成真实域名
- [ ] Tab 键能从 skip link 开始正常走一遍，焦点框可见
- [ ] 浏览器 DevTools Console 无 CSP 报错

**安全**
- [ ] HTTPS 生效（平台自动签发，浏览器显示锁头）
- [ ] 响应头有 `X-Content-Type-Options: nosniff` 与 CSP
- [ ] 页面无任何第三方请求（Network 面板里应只有你自己的域名）

---

## 九、回滚

平台都保留每次部署的历史，回滚不需要改代码：

- **Cloudflare Pages**：Deployments → 选一个历史版本 → **Rollback to this deployment**
- **Vercel**：Deployments → 目标版本右侧 `⋯` → **Promote to Production**
- **Netlify**：Deploys → 目标版本 → **Publish deploy**
- **Git 层面**（改动引入了 bug 要撤代码）：
  ```powershell
  git revert <坏的那次 commit>   # 生成一个反向提交，历史保留，最安全
  git push
  ```
  > 推荐 `git revert` 而不是 `git reset --hard`：前者可追溯、可再次撤销，后者会改写历史。

回滚后同样记得跑一次 `node scripts/release.mjs`，让 `version.json` 的版本号也回退，否则前端会一直提示「有新版本」。

---

## 十、排错手册

| 现象 | 根因 | 处理 |
|---|---|---|
| **推送后仓库里只有一个 README.md，其它文件都没上去** | **建仓库时勾了 "Add a README file"**，远程多出一个不相干的 `Initial commit`，`git push` 被拒 | 再跑一次 `scripts/connect-github.ps1 -User … -Repo …`，它会识别并询问是否覆盖；或手动 `git fetch origin main` 后 `git push --force-with-lease origin main` |
| 换了文件但线上还是旧的 | HTML 被缓存，或平台没重新部署 | `curl -I` 看 `cache-control`；确认 `_headers` 生效、平台 Deployments 里有新记录 |
| 前端一直不弹「发现新版本」 | 忘了跑 `release.mjs`，`version.json` 没变 | 跑一次脚本再推；`curl /version.json` 确认 `v` 变了。`verify-site.mjs` 已内置这条守卫，自检会直接拦下 |
| `version.json` 404 | 脚本没跑，或发布目录没设成 `site` | 检查平台 Output/Publish directory 配置 |
| 页面样式全乱 | CSP 拦掉了内联样式/脚本 | 本项目需要 `style-src 'unsafe-inline'` 与 `script-src 'unsafe-inline'`（游戏用了内联 `onclick`），别手改窄 |
| 内嵌试玩白屏、Console 报 `frame-src` | CSP 缺 `frame-src 'self'` | 已在 `_headers` / `vercel.json` 里配好，若自己改过请补回 |
| 平台构建失败 | 填了构建命令但项目没有构建步骤 | Build command **留空** |
| 拖拽上传后 `_headers` 不生效 | Netlify Drop 不读目录级配置 | 改用路径 B（Git 部署） |
| 手机底部被浏览器工具栏挡住 | 用了 `100vh` | 本项目已统一 `100dvh` + `safe-area-inset-bottom`，若自行改版请保持 |
| 域名打不开但平台域名正常 | DNS 未生效或 CNAME 填错 | `nslookup 你的域名` 看解析；DNS 生效最长可能要几小时 |

---

## 附：为什么这样设计

- **落地页与游戏同源**：`localStorage` 按源隔离，同源才能让「页内试玩」和「独立打开」共用同一份存档。
- **零第三方请求**：没有字体 CDN、没有统计脚本、没有图标库。落地页 25 KB、游戏 139 KB，一次请求就绪；也顺手满足了隐私与 CSP 收紧的需求。
- **点击才加载游戏**：首屏不执行 139 KB 的游戏脚本，LCP 更快；`aspect-ratio` 预留了位置，布局不会跳（CLS ≈ 0）。
- **CSP 里保留 `'unsafe-inline'`**：这是本项目的取舍——游戏的交互全部用内联 `onclick`，为了零依赖与单文件可移植性，牺牲了严格 CSP。如果哪天要做成多文件工程，再把事件绑定改成 `addEventListener`，就可以去掉 `'unsafe-inline'`。
