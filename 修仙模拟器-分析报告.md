# 《修仙模拟器 · 文字版》网页分析报告

- **分析对象**：`D:\ruanjian\WeChat\xwechat_files\wxid_e5li17quv8ci22_1573\msg\file\2026-09\xiuxian.html`
- **形态**：单文件 HTML（1882 行，约 60 KB），零依赖、零构建、纯离线
- **技术栈**：原生 HTML + CSS（自定义属性色板）+ 原生 JS（单个 `<script>`，`"use strict"`）
- **持久化**：`localStorage` 单键 `xiuxian_save_v1`
- **外部请求**：无（无 CDN、无字体、无统计脚本），可直接双击打开
- **题材**：文字向修仙放置/养成，13 大境界 41 层，从炼气一层到天道境

---

## 一、页面结构

三栏式布局（`.wrap` > `.top` + `.main`），`main` 为 `grid-template-columns: 330px 1fr`，高度锁 `100vh`。

| 区域 | 容器 | 内容 |
|---|---|---|
| 顶栏 | `.top` | 境界 / 修行日 / 灵石 / 战力 + 「道法」帮助 + 「重入轮回」硬重置 |
| 左栏（330px，可滚动） | `.left` | 道体（修为·气血·灵力三进度条 + 7 项属性）→ 法器（武器/防具/法宝×3）→ 坐骑 → 丹药 → 储物袋（40 格） |
| 右栏 | `.right` | 页签（修炼/妖兽/秘境/装备/坊市）→ 动作卡片区（`.actgrid`，自适应网格）→ 天机录日志 |
| 浮层 | `#modalRoot` / `.toast` | 帮助与飞升结局弹窗、操作提示 |

- 视觉语言统一：宣纸底纹（多层 `radial/repeating-linear-gradient` 叠加）、朱砂红 `#9e2b25` 主色、宋体做正文与标题（`.serif`），风格类与语义类分离（日志用 `.l-gan / .l-dmg / .l-epic` 等）。
- 战斗与秘境共用一个"动作面板"：`renderActions()` 按 `S.combat / S.dungeon` 状态切换到 `renderCombat()` / `renderDungeon()`，并把上排页签置灰（`.tab.dis`），防止状态穿越——这一点做得很干净。

## 二、状态与数据模型

单一全局状态对象 `S` 贯穿全局，是全站唯一数据源：

```
S = { level, exp, day, stones, hp, mp,
      equip:{ weapon, armor, mount, treasures[3] },
      bag[40], pills{回春丹,聚灵丹,破境丹}, logs[220],
      combat, dungeon, shop[4], shopMount, shopDay,
      buffBreak, autoEquipOn, kills, deaths, tab, ended }
```

派生属性全部现算（`stats()`），不缓存，因此不存在"属性不同步"的经典问题：

```
hpMax = 130 + 54×lv      mpMax = 65 + 27×lv
atk   = 13 + 5.2×lv      def   = 6 + 2.8×lv      crit = 5%
再加所有在身器物（武器/防具/坐骑/3 法宝）的属性
power = atk×2 + def×2.6 + hpMax×0.16 + mpMax×0.1 + crit×6
```

内容层是完全数据驱动的纯配置表：`QUALITIES`（5 品）、`BASE_NAMES`（法器名池）、`REALM_TREE`（境界树）、`PILLS`、`MONSTERS`（5 阶×5 名）、`GROUNDS`（5 历练地）、`SECRETS`（4 秘境）、`MOUNT_NAMES`（5 阶×5 名）。
**扩展内容只需往表里加一行**，不需要动逻辑——这是整个页面最值得称赞的架构决策。

## 三、数值系统速查

| 系统 | 公式 / 规则 |
|---|---|
| 修为需求 | `expNeed = floor(140 × 1.42^lv)`，lv0=140，lv40≈1.72 亿 |
| 打坐吐纳 | 1 日，`0.22 × expNeed × (1+修速%)`，回 18% 气血 / 28% 灵力 |
| 灵石修炼 | 1 日，`0.85 × expNeed`，耗 `25 + 6×lv` 灵石 |
| 闭关七日 | 7 日，`4.2 × expNeed`；26% 心魔（折损 42%）、16% 顿悟（×1.55）；需血灵双 90% |
| 突破成功率 | `clamp(84 − 2.8×大境界序号 + 破境丹18 + min(16, 修速×0.32), 22, 96)` |
| 突破失败 | 修为 −35%、气血剩 42% |
| 器物品质 | 凡/灵/宝/仙/神 = 属性倍率 1.0 / 1.7 / 2.9 / 4.8 / 8.2，暴击倍率更高 |
| 器物等级 | `lv = S.level + bias`，属性 `基础×品质倍率×(1+lv×0.34)` |
| 妖兽 | `hp 95、atk 11、def 3.4` 为基，乘 `(1+0.58×tier)×(1+0.30×lv)`，妖王 ×2.8/1.32/1.25 |
| 伤害 | `max(1, atk×rf(0.86,1.14) − def×0.6)`，暴击 ×1.85，灵技 ×2.3 |
| 坐骑 | 独有**遁速**：`切 clamp(speed,0,65)%` 缩短历练/秘境赶路，并提高脱身率 |
| 死亡惩罚 | 修为 −25%、3 日后醒来、气血灵力剩 30%、秘境中断清空 |

### 单位收益对比（以"当前境界所需修为"为 1）

| 途径 | 收益 |
|---|---|
| 打坐吐纳（1 日） | 0.22 |
| 闭关七日（合计 7 日） | 4.20 |
| 灵石修炼（1 日 + 灵石） | 0.85 |
| 斩凡兽 T0（单只） | 0.45 |
| 斩上古凶物 T4（单只） | 1.17 |
| 斩妖王 T4（单只） | 3.74 |
| 秘境单层（T3，均值） | 0.56 |
| 秘境贯通（T3，12 层合计） | ≈8.70 |

**结论：战斗与秘境才是主引擎，打坐只是回血手段。** 一趟天外仙宫全搜全推约折合 8.7 个当前境界的修为，而 `1.42^8.7 ≈ 20`，意味着理论上一次通关足够连升十几境。中后期节奏被"越级刷秘境"主导，而非数值表设计者预期的"坐关苦修"。

### 战力曲线上的一个关键判断

防御按 `−def×0.6` 线性减伤、伤害浮动仅 ±14%，属于**强属性压制模型**：属性一旦压过阈值，怪物伤害会被减到个位数。品质倍率跨度 8.2 倍，而境界每级只给 `atk +5.2`——所以**装备是战力的主引擎**，`tryEquipIfBetter` 用 `power` 单一标量排序在数值上是自洽的、选得对。

## 四、代码质量

**做得好的地方**

1. 单文件零依赖，双击即玩，状态机（无战斗 / 战斗中 / 秘境中）切换严谨，不会出现面板与状态错配。
2. 配置与逻辑彻底分离，新增境界、妖兽、秘境、法器只需改表。
3. `num()` 万/亿缩写、日志语义配色、`fadein`/`pop`/`tin` 动效克制，观感统一。
4. 存档做了版本键与**向后兼容**（旧存档缺 `shopMount` 自动补一头，缺 `treasures` 长度自动补齐）。

**问题清单（按严重度）**

| # | 位置 | 问题 |
|---|---|---|
| B1 | L1590 / L1594-1599 / L822 | **飞升结局永远触发不了（死代码）**。`ready = exp >= need && level < MAX_LV`，而天道境（lv40 = MAX_LV）时 `level < MAX_LV` 为假 → "白日飞升"卡片永不渲染；`doBreakthrough()` 里的 `if(level >= MAX_LV) ascend()` 也随之不可达。修法：`ready` 去掉 `level < MAX_LV` 判断，或对 `level === MAX_LV` 单出一张卡。 |
| B2 | L1341-1360 / L1458-1467 / L1555-1563 | **战斗中可绕过丹药上限**。战斗面板的「服丹」限制 3 次且消费一次怪物回合；但左栏丹药按钮在战斗中仍可点，直调 `usePill()`，既无次数限制、**也不触发怪物回合**，等于站着白回血。 |
| B3 | L1286-1339 | 战斗/秘境中左栏的「装备 / 卸下 / 弃」仍生效，残血时可以临阵换装。 |
| B4 | L764-789 + L640-646 | `advance()` 每日回 7% 气血 / 9% 灵力，而闭关先扣到 50%/20% 再走 7 日 → 出关时血回≈99%、灵回≈83%，**设计中的"闭关代价"实际不存在**（心魔分支的腰斩除外）。 |
| B5 | L823-824 | 修为无上限、突破后直接 `exp = 0`，溢出部分全额浪费。战斗给修为是"整份 need"量级，配合 B1 的卡点，后期收益大量白扔。 |
| B6 | L1377 / L1745-1748 | 每次动作、**每个怪物回合**都全量 `JSON.stringify` 写 localStorage（含装备、背包 40 件、日志）。建议在 `advance` 后或按节流（如 300ms）写档。 |
| B7 | L703-706 / L1332-1339 | 没有出售/分解渠道，背包满 40 件后新道具被"随手丢弃"，后期只能手工点「弃」。建议加"一键分解换灵石"。 |
| B8 | L1509-1518 | `card()` 传空 fn 会生成 `onclick=""`（如 `actionsGear` 的战力卡）；建议支持 `opt.static`。 |
| B9 | L1354-1357 | 破境丹无场景限制也无重复提示，连点会白白消耗（`buffBreak` 被覆盖为 18，不叠加）。 |
| B10 | L1628-1630 | `estPower()` 只是按攻击力线性估算的装饰数字，未含 tier 的防御/暴击，与真实强度差得较远，会误导玩家选历练地。 |
| B11 | L1760 | 存档只留 90 条日志，而内存上限 220 条，读档后日志会缩水。 |
| B12 | L409-411 / L1509 | `itemLabel()` 与 `card()` 都把内容拼进 `innerHTML`、不做转义。当前无外部输入故无风险，但一旦接入用户命名（道号、自定义器物）即成 XSS 面。 |

## 五、改进建议（按优先级）

1. **先修 B1**——一个游戏拿不到结局，等于核心目标缺失，改动只有一行。
2. **修 B2/B3**：把左栏装备与丹药按钮在 `S.combat || S.dungeon` 时禁用（加 `.lock` 或直接不绑事件），并把战斗中的服丹统一收敛到 `fightPill()`（含回合代价与 3 次上限）。
3. **数值层面给"打坐"一个存在理由**：现在打坐 0.22/日远低于战斗，且闭关代价被 B4 抹平。可选做法是给连续打坐叠加"静修"加成，或让闭关的血灵消耗不被 `advance()` 补偿（先把扣血放到 `advance` 之后）。
4. **补经济闭环**：加"分解/卖出"（可按 `itemCost×0.3` 回收），既解决 B7 的背包爆仓，也让装备掉落有边际价值。
5. **修为溢出结转**：突破后保留 `exp - need`，或允许连续突破，顺带把 B5 与后期节奏理顺。
6. **性能**：`renderAll()` 全量 `innerHTML` 重建 9 个区块 + 写档，在日志与背包装满时会明显变重；可给 `renderLog()` 改增量追加、给 `save()` 加节流。
7. **可玩性**：加入成就/图鉴（收录 5 阶×5 名器物与坐骑）、事件随机池扩容、失败分支的正反馈（如突破失败后小幅增加下次成功率），目前"刷—爆—刷"的重复感在中后期较突出。

## 六、安全与隐私

无任何网络请求、无第三方脚本/字体/统计、无 Cookie、无 `eval`，仅写 `localStorage`。图片与外链为零，文件可安全离线打开与转发。

---

*报告基于对该 HTML 源码的静态分析（含公式复算），未实际运行；数值结论为按公式推算的期望值，实际体感受随机数影响。*
